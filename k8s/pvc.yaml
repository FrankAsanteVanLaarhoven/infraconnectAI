apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: isaac-assets-pvc
  namespace: physical-ai
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 500Gi
  storageClassName: fast-ssd
